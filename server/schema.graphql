type Query {
  investments: [Investment!]!
}

type Mutation {
  addInvestment(address: String!, price: Float, lease: Float): Investment
}

type Investment {
  id: ID!
  address: String!
  price: Float
  lease: Float
}
