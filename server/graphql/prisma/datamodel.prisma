type Investment {
  id: ID! @unique
  address: String!
  price: Float
  lease: Float
}

type Provider {
  id: ID! @unique
  name: String!
}

type User {
  id: ID! @unique
  email: String! @unique
  password: String!
}
