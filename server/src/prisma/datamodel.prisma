type User {
  id: ID! @unique
  email: String @unique
  name: String!
  lastname: String
  posts: [Post!]!
}

type Post {
  id: ID! @unique
  title: String!
  published: Boolean! @default(value: "false")
  author: User
}

type Investment {
  id: ID! @unique
  address: String!
  price: Float
}
